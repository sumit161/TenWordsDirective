import { TenWordDirective } from './ten-word.directive';

describe('TenWordDirective', () => {
  it('should create an instance', () => {
    const directive = new TenWordDirective();
    expect(directive).toBeTruthy();
  });
});
